package com.palm360.airport.util.Encode;

public interface EncodeFormat {
	/**
	 * 生成二维码接口（生成图片）
	 * @param path 要生成的路径
	 * @param content 要生成的内容
	 * @return 返回生成的路径和文件名
	 */
	String makeCode(String path,String content);
}
